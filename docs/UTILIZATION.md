# Utilization / wall model — moved

The utilization pyramid, the what-binds ordering (readout → operand movement →
command issue → then the bus), the request-count law, and the memory-die
wishlist now live in
**[RELATED_SYSTEMS.md](RELATED_SYSTEMS.md#5-the-wall-model)** (§5).
